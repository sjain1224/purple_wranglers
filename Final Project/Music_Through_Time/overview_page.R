library(shiny)

overview <- tabPanel(
  "Overview",
  titlePanel("Overview of American Music"),
  intro
)

intro <- p("Ever since Rock and Roll, American Music has been consistent. As in, it is consistently getting worse.")